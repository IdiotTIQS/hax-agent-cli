'use strict';

const {
  createBackup,
  listBackups,
  restoreBackup,
  restoreFromBackupDir,
  rotateBackups,
  createSnapshot,
  computeChecksum,
  generateBackupId,
  MANIFEST_FILENAME,
  BACKUP_DIR_PREFIX,
} = require('./backup');

const {
  MigrationRegistry,
  detectVersion,
  writeVersion,
  needsMigration,
  runMigrations,
  migrateV1toV2,
  createDefaultRegistry,
  VERSION_FILE,
  normalizeVersion,
  compareVersions,
} = require('./migration');

const {
  serializeSession,
  deserializeSession,
  serializeSettings,
  serializeMessages,
  toNdjson,
  fromNdjson,
  toCsv,
  fromCsv,
  SENSITIVE_KEYS,
  SENSITIVE_PATTERNS,
} = require('./serializer');

module.exports = {
  createBackup,
  listBackups,
  restoreBackup,
  restoreFromBackupDir,
  rotateBackups,
  createSnapshot,
  computeChecksum,
  generateBackupId,
  MANIFEST_FILENAME,
  BACKUP_DIR_PREFIX,
  MigrationRegistry,
  detectVersion,
  writeVersion,
  needsMigration,
  runMigrations,
  migrateV1toV2,
  createDefaultRegistry,
  VERSION_FILE,
  normalizeVersion,
  compareVersions,
  serializeSession,
  deserializeSession,
  serializeSettings,
  serializeMessages,
  toNdjson,
  fromNdjson,
  toCsv,
  fromCsv,
  SENSITIVE_KEYS,
  SENSITIVE_PATTERNS,
};
